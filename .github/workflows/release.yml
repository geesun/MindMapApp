name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            args: '--target aarch64-apple-darwin'
            rust-targets: 'aarch64-apple-darwin'
            artifact-name: 'macos-arm64'
          - platform: macos-latest
            args: '--target x86_64-apple-darwin'
            rust-targets: 'x86_64-apple-darwin'
            artifact-name: 'macos-x64'
          - platform: macos-latest
            args: '--target universal-apple-darwin'
            rust-targets: 'aarch64-apple-darwin,x86_64-apple-darwin'
            artifact-name: 'macos-universal'
          - platform: ubuntu-22.04
            args: '--bundles deb'
            rust-targets: ''
            artifact-name: 'linux-deb'
          - platform: windows-latest
            args: ''
            rust-targets: ''
            artifact-name: 'windows-exe'

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-targets }}

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'
          key: ${{ matrix.artifact-name }}

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: Install npm dependencies
        run: npm install

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'MindMap ${{ github.ref_name }}'
          releaseBody: |
            ## MindMap ${{ github.ref_name }}

            ### Downloads
            - **macOS Apple Silicon (arm64)**: `MindMap_*_aarch64.dmg`
            - **macOS Intel (x86_64)**: `MindMap_*_x64.dmg`
            - **macOS Universal**: `MindMap_*_universal.dmg`
            - **Linux** (Debian/Ubuntu): `mindmap_*_amd64.deb`
            - **Windows**: `MindMap_*_x64-setup.exe`
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
